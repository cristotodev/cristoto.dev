---
import { books } from "@/data/books";
import PageLayout from "@/layouts/Base.astro";

const meta = {
    description:
        "Descubre mi colección de libros: recomendaciones, reseñas y lecturas que me han inspirado. Explora títulos de tecnología, desarrollo personal, negocios y más.",
    title: "Books",
};
---

<PageLayout meta={meta}>
    <h1 class="title mb-6">Libros</h1>
    <div class="prose prose-citrus max-w-none mb-8">
        <p>
            En esta sección encontrarás los libros que he leído y que seguiré
            leyendo. Me gusta explorar diferentes géneros, desde tecnología y
            programación hasta desarrollo personal y negocios. Cada libro ha
            dejado una huella en mi camino como desarrollador y como persona.
            ¡Espero que encuentres inspiración para tu próxima lectura!
        </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {
            books.map((book) => (
                <div class="rounded-2xl shadow-lg overflow-hidden shadow-lg hover:brightness-110 transition-all bg-gradient-to-r from-accent-one to-accent-two">
                    <img
                        src={book.cover}
                        alt={book.title}
                        class="w-full h-64 object-cover"
                    />
                    <div class="p-4">
                        <h2 class="text-xl font-bold mb-2 text-bgColor font-bold">
                            {book.title}
                        </h2>
                        <p class="text-sm text-bgColor font-semibold">
                            {book.author}
                        </p>
                    </div>
                </div>
            ))
        }
    </div>
</PageLayout>
